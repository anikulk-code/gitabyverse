{
  "name": "gitabyverse",
  "version": "0.1.0",
  "description": "Bhagavad Gita verse-by-verse lecture map — gitabyverse.com",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.30.4",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "npm run build:sitemap && react-scripts build",
    "build:gita-data": "node scripts/build-gita-data.js",
    "build:bannanje-data": "node scripts/build-bannanje-gita-source.js",
    "build:sitemap": "node scripts/generate-sitemap.js",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
